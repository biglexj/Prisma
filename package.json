{
  "name": "prisma",
  "private": true,
  "version": "1.0.6",
  "type": "module",
  "scripts": {
    "predev": "bun run scripts/free-port.ts 1421",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview",
    "tauri": "tauri",
    "icons:generate": "bun scripts/generate-icons.ts",
    "tauri:dev": "bun run icons:generate && tauri dev",
    "tauri:dev:isolated": "bun run icons:generate && tauri dev -- -- --dev --multi-instance",
    "tauri:build": "bun run icons:generate && tauri build",
    "posttauri:build": "bun scripts/copy-build-releases.ts",
    "release": "powershell -ExecutionPolicy Bypass -File ./scripts/release/build-release.ps1 -LocalOnly"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^5",
    "typescript": "^5.9",
    "vite": "^7"
  }
}

